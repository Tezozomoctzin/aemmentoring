  /rend01
    {
      # Hostname or IP of the render
      /hostname "{{ cq_host }}"
      # Port of the render
      /port "{{ cq_port }}"
      # Connect timeout in milliseconds, 0 to wait indefinitely
      # /timeout "{{ render_publish_connect_timeout }}"
    }
